
#include <stdio.h>

#define BUFSIZE 256

int a() {
    char buf[BUFSIZE];
    size_t buf_size = sizeof(buf);
    int t;
    float f;
    double d;
    return 0;
}

int main() {
    a();
    return 0;
}
