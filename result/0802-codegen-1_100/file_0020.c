
#include <stdio.h>
#include <stdlib.h>

int a() {
    int c = 100 * (rand() % 1000);

    if (c / 1000 / 100 - 1 + 2 * 2 < c) {
        for (; c > 0; c--) {
        }
    }
    
    return -1 > 0 % 5;
}

int main() {
    a();
    return 0;
}
